services:
  snapshot:
    build: .
    container_name: snapshot_app
    restart: unless-stopped
    ports:
      - '8080:8080'
    volumes:
      - ./downloads:/app/downloads
    networks:
      - caddy-net

networks:
  caddy-net:
    external: true
